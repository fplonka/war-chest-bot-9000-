# wp

Warrior Priest drawn coin is `Z_INFLIGHT` (private zone), not a continuation. Thirty minutes, golden8 knobs, `ladder_games=40`.

Horizon 0% at the end, 1,159 balanced solves/s. Swiss Elo vs Greedy at 0: init −149, then +180 / +376 / +530 / **+704** (95% CI about ±122). Monotone. Same shape as `seat` (+754); CIs overlap. Gate passed.
