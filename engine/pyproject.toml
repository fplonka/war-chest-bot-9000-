[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "warchest"
version = "0.1.0"
requires-python = ">=3.9"
description = "War Chest (2p ranked) rules engine"

[tool.maturin]
features = ["python"]
module-name = "warchest"
